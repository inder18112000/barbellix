{
  "name": "barbellix",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "mobile": "npm run start --workspace=@barbellix/mobile",
    "server": "npm run dev --workspace=@barbellix/server",
    "web": "npm run dev --workspace=@barbellix/web",
    "build": "npm run build --workspace=@barbellix/shared && npm run build --workspace=@barbellix/server && npm run build --workspace=@barbellix/web",
    "lint": "npm run lint --workspaces --if-present"
  },
  "devDependencies": {
    "typescript": "~5.9.2"
  }
}
